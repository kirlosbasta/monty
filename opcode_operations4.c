#include "monty.h"

/**
 * _stack - push element to the top of the list
 * @stack: Front of the stack
 * @line_number: line number
 *
 * Return: Nothing
 */
void _stack(UNUSED stack_t **stack, UNUSED unsigned int line_number)
{
	list.order = 1;
}

/**
 * _queue - push element to the rear of the list
 * @stack: Front of the stack
 * @line_number: line number
 *
 * Return: Nothing
 */
void _queue(UNUSED stack_t **stack, UNUSED unsigned int line_number)
{
	list.order = 2;
}
